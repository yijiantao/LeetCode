/*
 * @lc app=leetcode.cn id=389 lang=cpp
 *
 * [389] 找不同
 */

// @lc code=start
class Solution {
public:
    char findTheDifference(string s, string t) {
        /*  // 方法 一：
        sort(s.begin(), s.end());
        sort(t.begin(), t.end());

        for (int _index = 0; _index < s.size(); ++_index) {
            if (s[_index] != t[_index]) {
                return t[_index];
            }
        }
        
        return t[t.size()-1];
        */

    }
};
// @lc code=end

